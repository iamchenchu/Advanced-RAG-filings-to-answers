# TEMPLATE - the committed file holds placeholders; deploy/k8s/apply.sh
# substitutes real values at apply time. Never commit real credentials.
apiVersion: v1
kind: Secret
metadata:
  name: rag-secrets
  namespace: llm-rag
type: Opaque
stringData:
  DATABASE_URL: "__DATABASE_URL__"
