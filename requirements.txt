# Advanced-RAG-System — pinned to the versions verified on Python 3.14 (2026-08-18)

# API
fastapi==0.141.1
uvicorn[standard]==0.42.0
pydantic==2.13.4
pydantic-settings==2.12.0
python-dotenv==1.2.3

# Persistence
asyncpg==0.31.0

# Vector store
qdrant-client==1.19.0

# Ingestion
requests==2.34.2
httpx==0.28.1
beautifulsoup4==4.15.0
lxml==6.1.1
google-cloud-storage==3.13.1

# Embeddings / reranking (local CPU backend; TEI is used via HTTP in prod)
tokenizers==0.23.1
huggingface_hub==1.28.0
fastembed==0.8.0
numpy==2.5.2

# Observability
prometheus-client==0.24.1

# Tests
pytest==9.0.2
pytest-asyncio==1.3.0
